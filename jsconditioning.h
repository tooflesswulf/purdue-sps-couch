#if !defined(__JSCONDITIONING_H__)
#define __JSCONDITIONING_H__

double condition(double val, double pos_scale, double neg_scale, double pos_offset, double neg_offset, double pos_deadband, double neg_deadband);

#endif // __JSCONDITINING_H__
